fun exit(exitcode)
fun putint(intvalue)
fun putstr(straddr)

var arr = 10, 15, 13, 5, 6

fun main() =
    let
        var i = 0
    in
        while i < 5 do
            putint(arr[i]),
            i++
        end
    end,
    exit(0)